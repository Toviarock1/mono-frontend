export type LoginFormValues = {
    email: string;
    password: string;
    rememberMe: boolean;
}

export type SignupFormValues = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}